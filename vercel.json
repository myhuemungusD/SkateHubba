{
  "version": 2,
  "framework": "vite",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm -C client run build",
  "outputDirectory": "dist/public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
